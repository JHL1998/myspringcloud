server:
  port: 9020
spring:
  application:
    name: zipkinclient
  sleuth:
    web:
      client:
        enabled: true
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://localhost:9020/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
